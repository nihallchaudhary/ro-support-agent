name: ro_support_env
description: AI system for RO customer support and service booking
version: 1.0

tasks:
  - name: easy
  - name: medium
  - name: hard

action_space:
  reply: string
  issue_label: string
  book_service: boolean

observation_space:
  customer_query: string
  conversation_history: list
  step_count: integer
